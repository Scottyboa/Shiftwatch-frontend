{
  "name": "shiftwatch-calendar-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend-free responsive calendar editor for ShiftWatch config files.",
  "scripts": {
    "test": "node --test",
    "serve": "node scripts/serve.mjs"
  },
  "dependencies": {
    "js-yaml": "4.3.1"
  },
  "devDependencies": {
    "jsdom": "24.1.3"
  },
  "engines": {
    "node": ">=20"
  }
}
